__pycache__/
*.pyc
credenciais/
saida/
.cache-imagens/
*.lock
upload-state.json
alarmes.md

# Estrategia: numeros de canal, receita e planos de monetizacao.
# Este repo e PUBLICO (minutos de Actions) — documento de negocio fica local.
ESTRATEGIA-FANTASMA.md

# TikTok (local, nunca versionar — sessao logada e videos)
tiktok/biblioteca/
tiktok/poster/perfil_chrome/
tiktok/poster/*.log
tiktok/_tmp_render/

# Relatorios de sessao (NOITE-*.md): sao entregas para o Diego, com numeros de
# canal e ordem de prioridade. O que sobrevive deles vai para CLAUDE.md e
# CANAIS.md; o relatorio em si nao precisa viver num repo publico.
NOITE-*.md
